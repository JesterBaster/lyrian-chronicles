Art for the Foundry setup screen.

- `thumbnail.webp` — shown on the system tile in the setup list (roughly 600x400).

Add a `"background"` key to system.json pointing at a file here to set the
world-creation backdrop as well.
